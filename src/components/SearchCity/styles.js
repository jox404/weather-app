import { StyleSheet } from "react-native";

export const stylesSearchCity = StyleSheet.create({
  container: {
    width: "100%",
    height: "100%",
  },
  searchFieldContainer: {
    marginTop: 60,
    paddingHorizontal: 20,
  },
});
